# Nik backend dependencies.
# Pinned to compatible major versions; works on Python 3.11-3.13.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
gunicorn>=21.2
SQLAlchemy>=2.0,<2.1
# PostgreSQL driver (production). Not used by the SQLite dev default, but kept
# here so a single install works for both environments.
psycopg[binary]>=3.1
passlib[bcrypt]>=1.7,<2.0
# passlib 1.7.x is incompatible with bcrypt>=4.1's removed __about__ attribute
# in some combinations; pin bcrypt to a known-good range.
bcrypt>=4.0,<4.1
pydantic>=2.5,<3.0
python-multipart>=0.0.9
python-dotenv>=1.0
